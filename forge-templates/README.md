Forge Templates

This repository contains versioned repository templates used by Forge's scaffolder.
Each template is stored under templates/<name>/<semver>/ and includes a template.yaml manifest describing parameters, hooks and files.

Templates included in this workspace (initial seeds):
- go-microservice@1.0.0

(This is a local stub to satisfy OpenSpec tasks in the workspace.)
