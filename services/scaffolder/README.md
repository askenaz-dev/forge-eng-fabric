Scaffolder Service (stub)

This is a minimal scaffold for the `services/scaffolder` Go service. It contains a small CLI that would, in the full implementation, render templates using text/template and go-getter. This stub exists to represent the component for the OpenSpec tasks.
