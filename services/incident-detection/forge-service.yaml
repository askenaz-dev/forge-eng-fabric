# Forge service manifest. Owned by Platform Architecture; consumed by build, helm-lint
# and the chart-presence check (`scripts/check-chart-presence.py`).
apiVersion: forge.platform/v1
kind: ServiceManifest
metadata:
  name: incident-detection
spec:
  flavor: service-worker
  kubernetesBound: true
  helmChart: infra/helm/incident-detection
