package application

// All design-system HTTP routes are dispatched by the base Handler when
// `Handler.WithDesignSystem(...)` is set; see http.go. This file is kept as
// a placeholder so any external import of the package can reference it for
// godoc continuity.
