Forge Actions

This repository contains composite and reusable GitHub Actions used by Forge's CI baseline.
Each action is stored under actions/<name>/ and includes an action.yml describing inputs and runs.

Actions included in this workspace (initial seeds):
- forge/lint
